import { ArrowLeft } from "lucide-react";
import Link from "next/link";
import { notFound } from "next/navigation";
import { articles, type Locale } from "@/features/public-site/content";
export default async function ArticlePage({ params }: { params: Promise<{ locale: Locale; slug: string }> }) { const { locale, slug } = await params; const article = articles.find((item) => item.slug === slug); if (!article) notFound(); return <article className="mx-auto max-w-4xl px-5 py-12 sm:px-8 lg:py-20"><Link href={`/${locale}/blog`} className="inline-flex items-center gap-2 text-sm font-bold text-ink-muted hover:text-ink"><ArrowLeft className="size-4" />Dergiye dön</Link><p className="mt-12 text-[11px] font-bold uppercase tracking-[0.14em] text-gold-strong">{article.category} · {article.date}</p><h1 className="mt-4 font-display text-5xl font-semibold leading-[0.98] sm:text-6xl">{article.title}</h1><p className="mt-6 text-lg leading-8 text-ink-muted">{article.excerpt}</p><img src={article.image} alt="" className="mt-10 aspect-[16/9] w-full rounded-md object-cover" /><div className="mx-auto mt-10 max-w-2xl space-y-6 text-base leading-8 text-ink-muted"><p>Fethiye'de gayrimenkul kararı, yalnızca bir konut seçimi değildir. Bölgenin gün içindeki ritmi, ulaşım alışkanlıkları, mevsimsel hareketi ve uzun vadeli gelişimi; bir mülkün size nasıl hizmet edeceğini belirler.</p><h2 className="font-display text-3xl font-semibold text-ink">Doğru çerçeve ile başlayın</h2><p>Önce yaşam hedefi ve yatırım hedefini ayırmak, ardından bunların kesiştiği yerleri görmek gerekir. Bersu danışmanları bu süreci mülk gösteriminden önce başlatır.</p><p>Bu yaklaşım, kısa listeyi küçültürken karar güvenini artırır ve işlemin her aşamasında daha net bir iletişim sağlar.</p></div></article>; }
